name = "goflux-telemetry"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# Optional: bind Analytics Engine dataset for telemetry storage
# [[analytics_engine_datasets]]
# binding = "ANALYTICS"
# dataset = "goflux_telemetry"

# Set TELEMETRY_TOKEN in Cloudflare dashboard Secrets for basic auth
# Set ANALYTICS_DATASET env var if using Analytics Engine
